module.exports = {
  plugins: {
    // The Tailwind PostCSS plugin moved to a separate package.
    // Use @tailwindcss/postcss instead of directly using `tailwindcss` here.
    '@tailwindcss/postcss': {},
    autoprefixer: {},
  }
}
